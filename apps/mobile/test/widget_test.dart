// Placeholder smoke test — the real feature tests live in test/features/.
// This file is kept to satisfy the default Flutter test runner.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
